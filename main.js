function btnempty(who){
  he = document.getElementByID(who);
  he.removeAttribute('style');
  yesclick(who);
};

function btnfull(who){
  he = document.getElementByID(who);
  he.setAttribute('style', "red");
  noclick(who);
};

function yesclick(who){
  he = document.getElementByID();
};

function noclick(who){

};

function tolink(who){

};
function rememberlink(who){
  he = document.getElementByID(who);
  ami = he.getAttribute()
  whoelse = ["nora", ]
  for (var i = 0; i < whoelse.length; i++) {
    if (ami != whoelse[i]){

    };
  }
};
